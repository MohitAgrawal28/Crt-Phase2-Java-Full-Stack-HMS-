spring.application.name=demo
server.port=${PORT:8080}

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://dpg-d89ibijbc2fs73f8qicg-a/hms_db_4itu
spring.datasource.username=hms_db_4itu_user
spring.datasource.password=agrawalmm_3
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2

# Disable automatic schema initialization (HMS handles it)
spring.sql.init.mode=never
